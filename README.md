# ExcelViewer

Windows Forms app to import Excel (.xlsx/.xlsm) via ClosedXML, preview client data,
map fields, and run basic validation (duplicates & missing IDs).

## Build & Run
```powershell
dotnet restore
dotnet run
